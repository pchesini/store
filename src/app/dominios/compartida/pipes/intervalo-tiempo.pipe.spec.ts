import { IntervaloTiempoPipe } from './intervalo-tiempo.pipe';

describe('IntervaloTiempoPipe', () => {
  it('create an instance', () => {
    const pipe = new IntervaloTiempoPipe();
    expect(pipe).toBeTruthy();
  });
});
